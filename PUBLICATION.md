# Publication Notes

This repository is the public project scaffold for LLM Wiki.

Private runtime data is intentionally excluded:

- `.env` and credentials
- `data/` service volumes
- `raw/` captured source material
- generated personal wiki pages
- queue state and agent-run logs

Use `.env.example`, `docs/`, `openclaw/contracts/`, and `ops/` to understand the implementation.
